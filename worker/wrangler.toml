name = "hn-news-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# These are set via `npx wrangler secret put <KEY>` — never in this file
# Required secrets:
#   TWILIO_ACCOUNT_SID
#   TWILIO_AUTH_TOKEN
#   TWILIO_WHATSAPP_FROM
